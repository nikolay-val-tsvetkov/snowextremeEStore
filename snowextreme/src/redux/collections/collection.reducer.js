import SHOP_DATA from '../../pages/shop/shop.data';
import { GET_COLLECTION } from './collection.types';

const INITIAL_STATE = SHOP_DATA;
const collectionReducer = (state = INITIAL_STATE, action) => {
  return state;
};

export default collectionReducer;
